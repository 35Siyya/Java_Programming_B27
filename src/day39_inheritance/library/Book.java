package day39_inheritance.library;

public class Book {
    String title;    // they are instance variables for the sub class)
    String author;   // they are instance variables for the sub class)
    int chapters;    // they are instance variables for the sub class)
    int numberOfPages;  // they are instance variables for the sub class)
    boolean hasMovie;   // they are instance variables for the sub class)

}
